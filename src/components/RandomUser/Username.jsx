import React from "react";

const Username = ({ username }) => {
  return (
    <h2 className="text-center font-dmsans text-xs leading-4 tracking-tighter">{`${username}`}</h2>
  );
};

export default Username;
